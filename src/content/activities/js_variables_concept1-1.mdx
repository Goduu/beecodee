---
type: Activity
id: js_variables_concept1-1
language: javascript
tags: [beginner, variables]
question:
  type: PairMatchingQuestion
  description: "Variables are used to store data. Match the following declarations with their description."
  pairMatchingOptions:
    - content: "var foo = 5"
      oType: "code"
      position: "up"
    - content: "let foo = 10"
      oType: "code"
      position: "up"
    - content: "const foo = 10"
      oType: "code"
      position: "up"
    - content: "Declares a variable and assigns the value `5` to it."
      oType: "text"
      position: "down"
    - content: "Declares a variable and assigns the value `10` to it which can be reassigned."
      oType: "text"
      position: "down"
    - content: "Declares a variable and assigns the constant value `10` to it."
      oType: "text"
      position: "down"
  correctAnswer: [["var foo = 5", "Declares a variable and assigns the value `5` to it."], ["let foo = 10", "Declares a variable and assigns the value `10` to it which can be reassigned."], ["const foo = 10", "Declares a variable and assigns the constant value `10` to it."]]
---
