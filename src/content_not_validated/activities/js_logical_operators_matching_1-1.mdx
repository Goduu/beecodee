---
type: Activity
id: js_logical_operators_matching_1-1
language: javascript
tags: [beginner, logical_operators, matching]
question:
  type: CodeMatching
  description: "Match each code snippet with its description of how logical operators are used."
  options:
    - content: "if (idade > 18 && possuiCarteira) { console.log('Pode dirigir!'); }"
      oType: "text"
    - content: "if (temMaça || temBanana) { console.log('Você tem frutas!'); }"
      oType: "text"
    - content: "if (idade > 18 || possuiCarteira) { console.log('Pode dirigir!'); }"
      oType: "text"
    - content: "if (temMaça && temBanana) { console.log('Você tem frutas!'); }"
      oType: "text"
  correctAnswer: [
    ["if (idade > 18 && possuiCarteira) { console.log('Pode dirigir!'); }", "This code checks if both conditions (age greater than 18 and having a driver's license) are true."],
    ["if (temMaça || temBanana) { console.log('Você tem frutas!'); }", "This code checks if at least one of the conditions (having apples or bananas) is true."],
    ["if (idade > 18 || possuiCarteira) { console.log('Pode dirigir!'); }", "This code checks if at least one of the conditions (age greater than 18 or having a driver's license) is true."],
    ["if (temMaça && temBanana) { console.log('Você tem frutas!'); }", "This code checks if both conditions (having apples and bananas) are true."]
  ]
---
