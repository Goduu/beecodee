---
type: Activity
id: js_logical_operator_and_implementation1-1
language: javascript
tags: [beginner, logical_operators, conditions]
question:
  type: CodeImplementation
  description: "Write a code snippet using the AND (&&) operator to check if a user is eligible for a discount. The user is eligible if they are a member and have made a purchase of at least $50."
  codeSnippet: |
    let isMember = true;
    let purchaseAmount = 60;

    if (isMember && purchaseAmount >= 50) {
        console.log("Você é elegível para um desconto!");
    } else {
        console.log("Você não é elegível para um desconto.");
    }
  correctAnswer: |
    let isMember = true;
    let purchaseAmount = 60;

    if (isMember && purchaseAmount >= 50) {
        console.log("Você é elegível para um desconto!");
    } else {
        console.log("Você não é elegível para um desconto.");
    }
---
