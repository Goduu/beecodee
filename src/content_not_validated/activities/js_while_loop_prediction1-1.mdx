---
type: Activity
id: js_while_loop_prediction1-1
language: javascript
tags: [intermediate, prediction, loops]
question:
  type: CodeOutputPrediction
  description: "Predict the output of the following code snippet when the secret number is 3 and the user inputs 4."
  codeSnippet: |
    let numeroSecreto = 3;
    let chute;

    while (chute != numeroSecreto) {
        chute = prompt('Escolha um número entre 1 e 10');
        if (chute == numeroSecreto) { 
            alert(`Você descobriu o número secreto ${numeroSecreto}`);
        } else {
            if (chute > numeroSecreto) {
                alert(`O número secreto é menor que ${chute}`);
            } else {
                alert(`O número secreto é maior que ${chute}`);
            }
        }
    }
  options:
    - content: "The alert will display 'O número secreto é menor que 4'."
      oType: "text"
    - content: "The alert will display 'Você descobriu o número secreto 3'."
      oType: "text"
    - content: "The alert will display 'O número secreto é maior que 4'."
      oType: "text"
  correctAnswer: ["The alert will display 'O número secreto é menor que 4'."]
---
