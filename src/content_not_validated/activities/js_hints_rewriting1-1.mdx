---
type: Activity
id: js_hints_rewriting1-1
language: javascript
tags: [intermediate, hints, code_rewriting]
question:
  type: CodeRewriting
  description: "Update the `app.js` code to provide hints when the user’s guess is incorrect. Implement conditions to show if the secret number is higher or lower."
  codeSnippet: |
    // código omitido

    } else {
        if (numeroSecreto > chute) {
            alert(`O número secreto é maior que ${chute}`);
        } else {
            alert(`O número secreto é menor que ${chute}`);
        }
    }
  correctAnswer: |
    // código omitido

    } else {
        if (numeroSecreto > chute) {
            alert(`O número secreto é maior que ${chute}`);
        } else {
            alert(`O número secreto é menor que ${chute}`);
        }
    }
---
