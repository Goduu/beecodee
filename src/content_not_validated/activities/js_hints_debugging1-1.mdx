---
type: Activity
id: js_hints_debugging1-1
language: javascript
tags: [intermediate, debugging, hints]
question:
  type: Debugging
  description: "Identify the error in the following code snippet for providing hints and suggest a correction."
  codeSnippet: |
    // código omitido

    } else {
        if (chute > numeroSecreto) {
            alert(`O número secreto é menor que ${chute}`);
        } else {
            alert(`O número secreto é maior que ${chute}`);
        }
    }
  expectedOutput: "If the secret number is 5 and the user guesses 4, the hint should be 'O número secreto é maior que 4'."
  correctAnswer: |
    Error: The logic is inverted. The condition for hinting should be corrected.
    Correction: The message for 'maior' should be shown if `numeroSecreto` is greater than `chute`, and vice versa.
---
