---
type: Activity
id: js_logical_operator_or_implementation1-1
language: javascript
tags: [beginner, logical_operators, conditions]
question:
  type: CodeImplementation
  description: "Write a code snippet using the OR (||) operator to check if a user has either a VIP membership or a coupon code. The user gets a special offer if they have either of them."
  codeSnippet: |
    let hasVIPMembership = false;
    let hasCouponCode = true;

    if (hasVIPMembership || hasCouponCode) {
        console.log("Você tem uma oferta especial!");
    } else {
        console.log("Sem oferta especial.");
    }
  correctAnswer: |
    let hasVIPMembership = false;
    let hasCouponCode = true;

    if (hasVIPMembership || hasCouponCode) {
        console.log("Você tem uma oferta especial!");
    } else {
        console.log("Sem oferta especial.");
    }
---
