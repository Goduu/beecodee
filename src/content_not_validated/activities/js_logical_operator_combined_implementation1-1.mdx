---
type: Activity
id: js_logical_operator_combined_implementation1-1
language: javascript
tags: [intermediate, logical_operators, conditions]
question:
  type: CodeImplementation
  description: "Create a code snippet that combines AND (&&) and OR (||) operators to determine if a user is eligible for a special event. The user is eligible if they are either a member who has made a purchase or they have a special invitation."
  codeSnippet: |
    let isMember = true;
    let madePurchase = false;
    let hasSpecialInvitation = true;

    if ((isMember && madePurchase) || hasSpecialInvitation) {
        console.log("Você está elegível para o evento especial!");
    } else {
        console.log("Você não está elegível para o evento especial.");
    }
  correctAnswer: |
    let isMember = true;
    let madePurchase = false;
    let hasSpecialInvitation = true;

    if ((isMember && madePurchase) || hasSpecialInvitation) {
        console.log("Você está elegível para o evento especial!");
    } else {
        console.log("Você não está elegível para o evento especial.");
    }
---
