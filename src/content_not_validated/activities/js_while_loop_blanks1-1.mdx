---
type: Activity
id: js_while_loop_blanks1-1
language: javascript
tags: [intermediate, fill_in_the_blanks, loops]
question:
  type: FillInTheBlankQuestion
  description: "Complete the following code to use a `while` loop that continues prompting the user until they guess the secret number correctly."
  options:
    - content: "while (_____ != numeroSecreto) { chute = prompt('Escolha um número entre 1 e 100'); }"
      oType: "text"
    - content: "if (chute == numeroSecreto) { alert('Você descobriu o número secreto'); } else { alert('O número secreto é _____ que ${chute}'); }"
      oType: "text"
  correctAnswer: [
    "while (chute != numeroSecreto) { chute = prompt('Escolha um número entre 1 e 100'); }",
    "if (chute == numeroSecreto) { alert('Você descobriu o número secreto'); } else { alert('O número secreto é maior que ${chute}'); }"
  ]
---
