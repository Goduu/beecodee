---
type: Activity
id: js_template_strings1-1
language: javascript
tags: [beginner, template_strings]
question:
  type: CodeRewriting
  description: "Rewrite the code to correctly display the number secret using template strings."
  codeSnippet: |
    let numeroSecreto = 8;
    alert('Isso ai! Você descobriu o número secreto numeroSecreto');
  correctAnswer: |
    ```javascript
    let numeroSecreto = 8;
    alert(`Isso ai! Você descobriu o número secreto ${numeroSecreto}`);
    ```
---
