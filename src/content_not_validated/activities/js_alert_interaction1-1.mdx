---
type: Activity
id: js_alert_interaction1-1
language: javascript
tags: [beginner, interaction, alerts]
question:
  type: FillInTheBlankQuestion
  description: "To enhance user interaction, replace the console message with an alert when the guess is correct."
  options: 
    - content: "if (guess == secretNumber) { alert('You guessed correctly!'); } else { alert('You guessed wrong :('); }"
      oType: "text"
    - content: "if (guess == secretNumber) { console.log('You guessed correctly!'); } else { console.log('You guessed wrong :('); }"
      oType: "text"
    - content: "if (guess == secretNumber) { alert('You guessed wrong :('); } else { alert('You guessed correctly!'); }"
      oType: "text"
  correctAnswer: ["if (guess == secretNumber) { alert('You guessed correctly!'); } else { alert('You guessed wrong :('); }"]
  segments:
    - type: "Segment"
      sType: "text"
      content: "To provide immediate feedback with alerts, write:"
    - type: "Segment"
      sType: "gap"
      content: "40"
---
