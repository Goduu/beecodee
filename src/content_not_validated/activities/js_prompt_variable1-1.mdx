---
type: Activity
id: js_prompt_variable1-1
language: javascript
tags: [beginner, variables, user_input]
question:
  type: FillInTheBlankQuestion
  description: "To capture a number input from the user and store it in a variable, we use the `prompt` function and assign its value to a variable."
  options: 
    - content: "let userInput = prompt('Enter a number:');"
      oType: "text"
    - content: "let prompt = 'Enter a number';"
      oType: "text"
    - content: "prompt('Enter a number:');"
      oType: "text"
  correctAnswer: ["let userInput = prompt('Enter a number:');"]
  segments:
    - type: "Segment"
      sType: "text"
      content: "To store the user input in a variable called `chute`, we write: "
    - type: "Segment"
      sType: "gap"
      content: "30"
---
