---
type: Activity
id: js_attempt_tracking_blanks1-1
language: javascript
tags: [intermediate, fill_in_the_blanks, variables]
question:
  type: FillInTheBlankQuestion
  description: "Complete the following code to properly track and display the number of attempts."
  options:
    - content: "let attempts = 1; // Start with 1 since the first attempt is made immediately\n\nwhile (chute != numeroSecreto) {\n    chute = prompt('Escolha um número entre 1 e 100');\n    if (chute == numeroSecreto) {\n        alert(`Isso ai! Você descobriu o número secreto ${numeroSecreto} com ${attempts} attempts`);\n    } else {\n        // Increment attempts here\n        attempts = _____ + 1;\n    }\n}"
      oType: "code"
    - content: "attempts = _____;\n"
      oType: "text"
  correctAnswer: [
    "attempts = attempts + 1;",
    "attempts++;"
  ]
---
