---
type: Activity
id: js_testing_conditions1-1
language: javascript
tags: [beginner, conditionals, testing]
question:
  type: FillInTheBlankQuestion
  description: "If the user's guess is correct, the code will log a success message to the console. Otherwise, nothing will happen."
  options: 
    - content: "if (chute == numeroSecreto) { console.log('Correct!'); }"
      oType: "text"
    - content: "if (chute != numeroSecreto) { console.log('Correct!'); }"
      oType: "text"
    - content: "if (chute === numeroSecreto) { console.log('Correct!'); }"
      oType: "text"
  correctAnswer: ["if (chute == numeroSecreto) { console.log('Correct!'); }"]
  segments:
    - type: "Segment"
      sType: "text"
      content: "To log a success message when the guess is correct, we write: "
    - type: "Segment"
      sType: "gap"
      content: "30"
---
