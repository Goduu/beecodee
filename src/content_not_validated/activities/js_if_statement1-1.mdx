---
type: Activity
id: js_if_statement1-1
language: javascript
tags: [beginner, conditionals, logic]
question:
  type: FillInTheBlankQuestion
  description: "In the code below, we check if the user's guess matches the secret number."
  options: 
    - content: "if (chute == numeroSecreto) { console.log('Correct!'); }"
      oType: "text"
    - content: "if (chute = numeroSecreto) { console.log('Correct!'); }"
      oType: "text"
    - content: "if (chute === numeroSecreto) { console.log('Correct!'); }"
      oType: "text"
  correctAnswer: ["if (chute == numeroSecreto) { console.log('Correct!'); }"]
  segments:
    - type: "Segment"
      sType: "text"
      content: "To check if the user's guess is correct, we write: "
    - type: "Segment"
      sType: "gap"
      content: "30"
---
